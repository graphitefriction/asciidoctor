= Customize the Table of Contents Title
//following sections from toc-custom.adoc

You can change the title of the Table of Contents (TOC) with the `toc-title` attribute.

== Set `toc-title`

To generate a TOC with a custom title, set the `toc-title` attribute and assign it your preferred title.

.Define a new TOC title
[source]
----
include::example$toc-title.adoc[]
----
<1> The `toc` attribute must be set in order to use `toc-title`.
<2> The `toc-title` is set and assigned the value `Table of Adventures` in the document's header.

.Result: Define a new TOC title
====
#Fix ex#
//image::toc-title.png[Table of contents title]
====
